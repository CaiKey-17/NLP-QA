# cd vào src, chạy lệnh:
python -m venv venv 

# Sau đó chạy lệnh:
.\venv\Scripts\activate 

# cd vào demo, chạy lệnh:
pip install -r requirements.txt  

# Sau đó, chạy lệnh:
python -m streamlit run app.py 